resources:
  schemas:
    pixels_schema:
      name: ${var.schema}
      catalog_name: ${var.catalog}
      comment: "Pixels Medical Imaging solution accelerator"

  volumes:
    pixels_volume:
      name: ${var.volume_name}
      catalog_name: ${var.catalog}
      schema_name: ${var.schema}
      comment: "DICOM files, segmentation exports, and STOW uploads"
